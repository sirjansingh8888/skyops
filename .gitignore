# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/
# Secrets
.env
# Data and weights (re-download with scripts/download_data.py)
data/raw/
data/processed/
# trained SkyOps weights in models/ ARE tracked (all under 60 MB) so a fresh clone runs fully trained;
# training runs and pretrained checkpoints downloaded to the repo root are not
runs/
/weights/
/*.pt
*.pth
*.onnx
*.zip
*.log
# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
